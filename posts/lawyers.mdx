---
title: My title
date: 
tags: early-stage-startup
---



import { useState } from 'react'

# Here's a button, click it!


export var exampleImage = 'https://example.com/image.jpg'

export const MyDynamicButton = () => {
  const [count, setCount] = useState(0)
  return (
      <>
      <button onClick={() => setCount(count + 1)}>Clicked {count} times!</button>
      <button onClick={() => myState.apple = "hey there"}>Change title</button>
      </>
  )
}

Click it! <MyDynamicButton />
{JSON.stringify(frontmatter)}




# Lawyers and Early Stage Startups 

# {frontmatter.title}


{props.myState.apple}<br/>
{exampleImage}<br/>


What will you need lawyers for

* Incorporation
* Fundraising events 




export const dog = "dog";



## Incorporation

* Just use Clerky standard docs. May be a little more time consuming than a lawyer but 1. you will learn and 2. it will be much much cheaper



## Fundraising

### Seed

You're probably

